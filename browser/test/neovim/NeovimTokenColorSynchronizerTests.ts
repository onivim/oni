/**
 * NeovimTokenColorSynchronizerTests.ts
 */

import * as assert from "assert"

describe("NeovimTokenColorSynchronizer", () => {
    describe("synchronizeTokenColors", () => {
        it("adds token color if it doesn't exist", () => {
            assert.ok(false, "fails")
        })

        it("doesn't add token color if it already exists", () => {
            assert.ok(false, "fails")
        })

        it("updates token color if it changed", () => {
            assert.ok(false, "fails")
        })
    })

    describe("getHighlightGroupForTokenColor", () => {
        it("returns highlight group even if not synchronized yet", () => {
            assert.ok(false, "fails")
        })

        it("returns same highlight group as from synchronize call", () => {
            assert.ok(false, "fails")
        })
    })
})
