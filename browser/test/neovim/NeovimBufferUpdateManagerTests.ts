/**
 * NeovimBufferUpdateManagerTests.ts
 */

import * as assert from "assert"

import { NeovimBufferUpdateManager } from "./../../src/neovim/NeovimBufferUpdateManager"

describe("NeovimBufferUpdateManagerTests", () => {
    it("tests", async () => {
        assert.ok(false, "fail")
    })
})
